# The metric itself (script_metric.py, script_betting.py) needs only numpy and pandas.
numpy>=1.24
pandas>=2.0

# The reproduction pipeline additionally needs:
matplotlib>=3.7
scipy>=1.10

# Optional: nltk (punkt) reproduces Zhan et al.'s sentence split in
# pipeline/external/empathy_checks.py; a regex splitter is used otherwise.
# nltk>=3.8
